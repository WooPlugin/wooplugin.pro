# WooPlugin.pro Environment Variables
# Copy to .env for local development
# Set these in Vercel dashboard for production

# LemonSqueezy test mode (set to "true" for local dev, omit or "false" for production)
LEMONSQUEEZY_TEST_MODE=true

# LemonSqueezy API key (for server-side API calls)
# Create at: https://app.lemonsqueezy.com/settings/api
LEMONSQUEEZY_API_KEY=your-api-key-here

# GitHub token for downloading Pro plugins from private repos (REQUIRED)
# Create at: https://github.com/settings/tokens?type=beta (fine-grained token)
# Repository access: WooPlugin/product-feed-for-woocommerce-pro, WooPlugin/accessibility-scanner-pro
# Permissions: Contents -> Read-only
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxx

# Secret for signing time-limited download tokens
# Generate with: openssl rand -hex 32
DOWNLOAD_TOKEN_SECRET=your-random-secret-here

# Upstash Redis for rate limiting (REQUIRED)
# Auto-added by Vercel KV integration, or get from Upstash console
KV_REST_API_URL=https://your-db.upstash.io
KV_REST_API_TOKEN=your-token-here
